deployment:
  hostNetwork: false

service:
  enabled: true
  type: LoadBalancer

ports:
  web:
    port: 80

  websecure:
    port: 443

additionalArguments:
  - "--entrypoints.web.address=:80"
  - "--entrypoints.websecure.address=:443"
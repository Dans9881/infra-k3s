deployment:
  hostNetwork: true

service:
  enabled: false

ports:
  web:
    port: 80
    hostPort: 80

  websecure:
    port: 443
    hostPort: 443

additionalArguments:
  - "--entrypoints.web.address=:80"
  - "--entrypoints.websecure.address=:443"